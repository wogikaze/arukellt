{
  "name": "arukellt",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "playground:build": "npm run build:app --prefix playground",
    "playground:serve": "npm run serve --prefix playground",
    "serve": "npm run playground:serve"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
