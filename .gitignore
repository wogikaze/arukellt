.worktrees/
wt/
target/

# Python bytecache
__pycache__/
*.pyc

# instant-grep index
.ig/

# playwright 2e2 test
.playwright-mcp

# io/fs test artifacts
_test_io.txt
test_output.txt

# compiled wasm artifacts in fixture directories
tests/fixtures/**/*.wasm
wasi_snapshot_preview1.reactor.wasm
tests/component-interop/**/*.wasm

# wasm build/test artifacts
*.wasm
*.cwasm

# Playground static assets (built in CI for Pages; run `npm run build:app` locally)
docs/playground/dist/
docs/playground/wasm/

# Allow the pinned-reference selfhost wasm (ADR-029 / #585). This is the
# trusted bootstrap base for the four selfhost gates; see bootstrap/PROVENANCE.md.
!bootstrap/*.wasm

# root-level scratch/temp files
/*.ark
/*.py
/*.sh
/_test_*.ark
/archive.zip
/check_layout

# WAT is a generated text format of WASM; not a source format in this project
*.wat

# s2 debug disassembly dumps
s2_disasm*
/audit-*.json

# benchmark build artifacts (result summaries are tracked separately)
benchmarks/*.wasm
benchmarks/*.bench.wasm
benchmarks/results/*.wasm

# local-only reference specs (not tracked; cause docs-gen drift)
docs/spec/spec-*/

# bootstrap verification scratch directories
.bootstrap-build/
.compare-outputs-scratch/

# local selfhost / tooling scratch outputs
.build/

# Node.js
**/node_modules/

# VS Code test downloads
**/.vscode-test/

# wasm-pack build output
crates/ark-playground-wasm/pkg/

# playground benchmarks output under docs (if generated locally)
docs/playground/dist/benchmarks/results/

# codex local settings
.codex
.codex-debug/
.mcp.json

# Cursor IDE settings
.cursor/

# Claude session state (lock files, worktrees)
.claude/

# Powers(力)
superpowers/
.worktrees
tmp/