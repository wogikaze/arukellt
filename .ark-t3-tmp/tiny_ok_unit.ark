
use std::io
fn main() {
    let w = io::writer_new()
    let d: Vec<i32> = Vec::new()
    let r = io::write_all(w, d)
    match r {
        Result::Ok(_) => println("ok"),
        Result::Err(_) => println("err"),
    }
}
