# trait なし環境での抽象化戦略

> **Archive / current-first note**:
> this document reflects an older design stage where traits were intentionally absent.
> It is kept for historical context only.
> Current syntax/type-system behavior has traits, methods, operator overloading, and nested generics in the current branch.

## How to read this file now

Use this document only for:

- understanding an earlier simplification strategy
- historical design rationale
- comparing old constraints with the current implementation

Do **not** use it as the source of truth for:

- current trait availability
- current method syntax support
- current operator support
- current stdlib abstraction surface

## Current-first replacement references

- `docs/current-state.md`
- `docs/language/syntax.md`
- `docs/language/type-system.md`
- `docs/compiler/pipeline.md`
