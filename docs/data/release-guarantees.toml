# Release guarantees SSOT (#770 Phase 2)
# Views: docs/release-criteria.md (normative), docs/release-checklist.md (executable),
#        docs/data/release-guarantees.md (generated matrix)
#
# Structure:
#   [[guarantees]] — contract definitions (what we promise at release time)
#   [[checks]]     — executable verification items (what we run to verify)
#
# A guarantee may be backed by one or more checks. A check may be
# release-blocking or informational. The release-blocker set is defined
# solely by checks with release_blocking = true — no supplemental lists.
#
# Guarantee fields:
#   cadence — every-pr | merge-to-main | nightly | release-only | manual
#             (when the guarantee's checks are actually run in CI)
#
# Check axes (do not overload a single field):
#   release_blocking  — bool: failure stops a tagged release
#   included_in_full  — bool: part of `verify full`
#   included_in_quick — bool: part of `verify quick`
#   result            — pass | fail | partial | not-run (test outcome)
#   freshness         — generated only; never stored in this file
#   incident_id       — required when result = "fail" and release_blocking = true
#   evidence_type     — smoke | static-scan | fixture-set | exhaustive | manual
#   last_verified_commit — git SHA of the commit where result was observed
#   verified_at       — date when the check was last run (for freshness derivation)
#   stale_after_days  — days after which freshness becomes "stale"
# Freshness is computed by the generator from verified_at and stale_after_days.
# A failed or partial result can still have fresh evidence.

[meta]
criteria_view = "docs/release-criteria.md"
checklist_view = "docs/release-checklist.md"
entry_view = "docs/release/entry.md"

# ── Guarantees (contract definitions) ─────────────────────────────────────────

[[guarantees]]
id = "compile_wasm32_gc"
tier = "guaranteed"
cadence = "every-pr"
summary = "arukellt compile --target wasm32-gc produces valid Wasm (CLI default)"
coverage = ["docs/examples/hello.ark", "target:wasm32-gc", "wasm-validation"]

[[guarantees]]
id = "compile_wasm32"
tier = "guaranteed"
cadence = "every-pr"
summary = "arukellt compile --target wasm32 produces valid Wasm (supported / AtCoder path)"
coverage = ["docs/examples/hello.ark", "target:wasm32", "wasm-validation"]
known_limitation = "Not the primary CI emphasis; still contract-stable"

[[guarantees]]
id = "run_wasmtime"
tier = "guaranteed"
cadence = "every-pr"
summary = "arukellt run executes via wasmtime"
coverage = ["tests/fixtures/hello_world.ark", "default-target", "wasmtime"]

[[guarantees]]
id = "fixture_harness"
tier = "guaranteed"
cadence = "every-pr"
summary = "Fixture harness passes for the current observed harness snapshot"
coverage = ["tests/fixtures/manifest.txt", "registered-harness-cases"]
known_limitation = "See fixture accounting: observed harness ≠ full manifest expansion"

[[guarantees]]
id = "determinism"
tier = "guaranteed"
cadence = "every-pr"
summary = "Same input → identical Wasm bytes"
coverage = ["scripts/check/check-release-determinism.sh::declared-corpus"]

[[guarantees]]
id = "no_panic_user_paths"
tier = "guaranteed"
cadence = "every-pr"
summary = "No panic on user-reachable CLI paths"
coverage = ["scripts/check/check-panic-audit.sh::user-reachable-scan"]

[[guarantees]]
id = "cli_check"
tier = "guaranteed"
cadence = "every-pr"
summary = "The check command rejects an invalid source without producing Wasm"
coverage = ["tests/fixtures/diagnostics/type_mismatch.ark", "diagnostic-exit-status"]
known_limitation = "Guarantee is command behavior, not acceptance of every valid program"

[[guarantees]]
id = "cli_init"
tier = "guaranteed"
cadence = "every-pr"
summary = "The init command generates every supported project template"
coverage = ["minimal", "cli", "with-tests", "wasi-host"]
known_limitation = "Template checks validate generated project structure and smoke behavior"

[[guarantees]]
id = "cli_doc"
tier = "guaranteed"
cadence = "every-pr"
summary = "The doc command resolves manifest symbols and generates HTML reference"
coverage = ["std/manifest.toml", "symbol-lookup", "html-generation"]
known_limitation = "Guarantee covers manifest lookup and generator contract"

[[guarantees]]
id = "cli_help"
tier = "guaranteed"
cadence = "every-pr"
summary = "The help command prints the canonical command surface"
coverage = ["src/compiler/main/usage.ark", "docs/data/cli-surface.toml"]
known_limitation = "Help output is checked against the structured command catalogue"

[[guarantees]]
id = "emit_component"
tier = "provisional"
cadence = "nightly"
summary = "compile --emit component / component build"
coverage = ["component-emit", "wasm32-gc"]
known_limitation = "Library exports need s2 wasm; ABI coverage incomplete; may use wasm-tools helpers"

[[guarantees]]
id = "lsp"
tier = "provisional"
cadence = "nightly"
summary = "LSP hover/completion/diagnostics"
known_limitation = "Feature set still evolving"

[[guarantees]]
id = "ark_toml"
tier = "provisional"
cadence = "nightly"
summary = "ark.toml project schema"
known_limitation = "Schema fields continue to grow"

[[guarantees]]
id = "dap"
tier = "experimental"
cadence = "manual"
summary = "ark-dap / debug-adapter"
known_limitation = "Scaffold only"

[[guarantees]]
id = "vscode_dap"
tier = "experimental"
cadence = "manual"
summary = "VS Code extension DAP wiring"
known_limitation = "Stub / evolving"

[[guarantees]]
id = "freestanding"
tier = "not_guaranteed"
cadence = "release-only"
summary = "wasm32-freestanding public target"
known_limitation = "ADR-007 retired / hard error"

[[guarantees]]
id = "native_targets"
tier = "not_guaranteed"
cadence = "release-only"
summary = "native-cpp / native-llvm"
known_limitation = "Scaffold; ABI undecided (ADR-045)"

# ── Checks (executable verification items) ────────────────────────────────────
# The release-blocker set is exactly {check | release_blocking = true}.
# No supplemental lists — release-checklist.md is generated from these entries.

[[checks]]
id = "check_compile_wasm32_gc"
guarantee_id = "compile_wasm32_gc"
command = "scripts/run/arukellt-selfhost.sh compile docs/examples/hello.ark --target wasm32-gc -o .build/release-checks/wasm32-gc.wasm"
ci_job = "verification"
release_blocking = true
included_in_full = true
included_in_quick = false
result = "pass"
evidence_type = "smoke"
verification_environment = "linux; repository verification toolchain"
last_verified_commit = "a80b4181"
verified_at = "2026-07-11"
stale_after_days = 30
platforms = ["linux"]

[[checks]]
id = "check_compile_wasm32"
guarantee_id = "compile_wasm32"
command = "scripts/run/arukellt-selfhost.sh compile docs/examples/hello.ark --target wasm32 -o .build/release-checks/wasm32.wasm"
ci_job = "verification"
release_blocking = true
included_in_full = true
included_in_quick = false
result = "pass"
evidence_type = "smoke"
verification_environment = "linux; repository verification toolchain"
last_verified_commit = "a80b4181"
verified_at = "2026-07-11"
stale_after_days = 30
platforms = ["linux"]

[[checks]]
id = "check_run_wasmtime"
guarantee_id = "run_wasmtime"
command = "scripts/run/arukellt-selfhost.sh run tests/fixtures/hello_world.ark"
ci_job = "verification"
release_blocking = true
included_in_full = true
included_in_quick = false
result = "pass"
evidence_type = "smoke"
verification_environment = "linux; repository verification toolchain"
last_verified_commit = "a80b4181"
verified_at = "2026-07-11"
stale_after_days = 30
platforms = ["linux"]

[[checks]]
id = "check_fixture_harness"
guarantee_id = "fixture_harness"
command = "python3 scripts/manager.py verify fixtures"
ci_job = "verification"
release_blocking = true
included_in_full = true
included_in_quick = false
result = "fail"
evidence_type = "fixture-set"
verification_environment = "linux; repository verification toolchain"
last_verified_commit = "2cd10f16"
verified_at = "2026-07-15"
stale_after_days = 30
affected_count = 367
incident_id = "incident_fixture_parity_367"
blocker_category = "fixture"
blocker_scope = "full"
blocker_owner = "compiler/runtime"
blocker_issue = "#807"
first_seen_commit = "89eb5eb4"
platforms = ["linux"]
note = "Failures in observed harness snapshot. Same incident as selfhost fixture-parity — not double-counted. See project-state.toml for current registry count."

[[checks]]
id = "check_determinism"
guarantee_id = "determinism"
command = "bash scripts/check/check-release-determinism.sh"
ci_job = "verification"
release_blocking = true
included_in_full = true
included_in_quick = false
result = "pass"
evidence_type = "smoke"
verification_environment = "linux; repository verification toolchain"
last_verified_commit = "a80b4181"
verified_at = "2026-07-11"
stale_after_days = 30
platforms = ["linux"]

[[checks]]
id = "check_no_panic"
guarantee_id = "no_panic_user_paths"
command = "bash scripts/check/check-panic-audit.sh"
ci_job = "verification"
release_blocking = true
included_in_full = true
included_in_quick = false
result = "pass"
evidence_type = "static-scan"
verification_environment = "linux; repository verification toolchain"
last_verified_commit = "a80b4181"
verified_at = "2026-07-11"
stale_after_days = 30
platforms = ["linux"]

[[checks]]
id = "check_cli_check"
guarantee_id = "cli_check"
command = "python3 scripts/check/check-cli-guarantees.py check"
ci_job = "verification"
release_blocking = true
included_in_full = true
included_in_quick = false
result = "pass"
evidence_type = "smoke"
verification_environment = "linux; repository verification toolchain"
last_verified_commit = "a80b4181"
verified_at = "2026-07-11"
stale_after_days = 30
platforms = ["linux"]

[[checks]]
id = "check_cli_init"
guarantee_id = "cli_init"
command = "python3 scripts/check/check-init-templates.py"
ci_job = "verification"
release_blocking = true
included_in_full = true
included_in_quick = false
result = "pass"
evidence_type = "smoke"
verification_environment = "linux; repository verification toolchain"
last_verified_commit = "a80b4181"
verified_at = "2026-07-11"
stale_after_days = 30
platforms = ["linux"]

[[checks]]
id = "check_cli_doc"
guarantee_id = "cli_doc"
command = "python3 scripts/check/check-manifest-doc.py"
ci_job = "verification"
release_blocking = true
included_in_full = true
included_in_quick = false
result = "pass"
evidence_type = "smoke"
verification_environment = "linux; repository verification toolchain"
last_verified_commit = "a80b4181"
verified_at = "2026-07-11"
stale_after_days = 30
platforms = ["linux"]

[[checks]]
id = "check_cli_help"
guarantee_id = "cli_help"
command = "python3 scripts/check/check-cli-guarantees.py help"
ci_job = "verification"
release_blocking = true
included_in_full = true
included_in_quick = false
result = "pass"
evidence_type = "smoke"
verification_environment = "linux; repository verification toolchain"
last_verified_commit = "a80b4181"
verified_at = "2026-07-11"
stale_after_days = 30
platforms = ["linux"]

[[checks]]
id = "check_close_gate_076"
guarantee_id = ""
command = "python3 scripts/check/check-false-done-close-gates.py"
ci_job = "verification"
release_blocking = true
included_in_full = false
included_in_quick = true
result = "pass"
evidence_type = "smoke"
verification_environment = "linux; repository verification toolchain"
last_verified_commit = "fd14539c23288d3ed993c03600aeed36cd478d06"
verified_at = "2026-07-14"
stale_after_days = 30
platforms = ["linux"]
note = "false-done close-gate enforcement passes; close-gate regression fixed"

[[checks]]
id = "check_t3_wasm_validate"
guarantee_id = ""
command = "python3 scripts/check/check-t3-wasm-validate.py"
ci_job = "verification"
release_blocking = true
included_in_full = false
included_in_quick = true
result = "fail"
evidence_type = "smoke"
verification_environment = "linux; repository verification toolchain"
last_verified_commit = "2cd10f16"
verified_at = "2026-07-15"
stale_after_days = 30
aggregate_failure_count = 1
fixture_failure_count = 213
affected_count = 1
incident_id = "incident_t3_wasm_validate"
blocker_category = "verification"
blocker_scope = "quick"
blocker_owner = "Wasm backend"
blocker_issue = "#808"
first_seen_commit = "fd14539c23288d3ed993c03600aeed36cd478d06"
platforms = ["linux"]
note = "T3 fixture Wasm validation: 1 aggregate gate failure + 213 fixture-level validate failures (func N failed to validate)"

# ── Full-verification checks (supplemental, previously in release-checklist.md) ──

[[checks]]
id = "check_selfhost_fixpoint"
guarantee_id = ""
command = "python3 scripts/manager.py selfhost fixpoint --build"
ci_job = "verification"
release_blocking = true
included_in_full = true
included_in_quick = false
result = "fail"
evidence_type = "exhaustive"
verification_environment = "linux; repository verification toolchain"
last_verified_commit = "2cd10f16"
verified_at = "2026-07-15"
stale_after_days = 30
affected_count = 1
incident_id = "incident_selfhost_fixpoint"
blocker_category = "bootstrap"
blocker_scope = "full"
blocker_owner = "selfhost compiler"
blocker_issue = "#813"
first_seen_commit = "a80b4181"
platforms = ["linux"]
note = "Stage 2 and Stage 3 compiler hashes differ; fixpoint not reached"

[[checks]]
id = "check_selfhost_cli_parity"
guarantee_id = ""
command = "python3 scripts/manager.py selfhost parity --mode --cli"
ci_job = "verification"
release_blocking = true
included_in_full = true
included_in_quick = false
result = "fail"
evidence_type = "smoke"
verification_environment = "linux; repository verification toolchain"
last_verified_commit = "2cd10f16"
verified_at = "2026-07-15"
stale_after_days = 30
affected_count = 2
incident_id = "incident_selfhost_cli_parity"
blocker_category = "bootstrap"
blocker_scope = "full"
blocker_owner = "selfhost CLI"
blocker_issue = "#811"
first_seen_commit = "a80b4181"
platforms = ["linux"]
note = "CLI parity drifts for --help and compose --validate"

[[checks]]
id = "check_selfhost_diag_parity"
guarantee_id = ""
command = "python3 scripts/manager.py selfhost diag-parity"
ci_job = "verification"
release_blocking = true
included_in_full = true
included_in_quick = false
result = "fail"
evidence_type = "smoke"
verification_environment = "linux; repository verification toolchain"
last_verified_commit = "2cd10f16"
verified_at = "2026-07-15"
stale_after_days = 30
affected_count = 3
incident_id = "incident_selfhost_diag_parity"
blocker_category = "bootstrap"
blocker_scope = "full"
blocker_owner = "selfhost diagnostics"
blocker_issue = "#812"
first_seen_commit = "a80b4181"
platforms = ["linux"]
note = "Selfhost diagnostic parity differs from Rust host compiler"

[[checks]]
id = "check_wat_roundtrip"
guarantee_id = ""
command = "bash scripts/run/wat-roundtrip.sh"
ci_job = "verification"
release_blocking = true
included_in_full = true
included_in_quick = false
result = "fail"
evidence_type = "smoke"
verification_environment = "linux; repository verification toolchain"
last_verified_commit = "2cd10f16"
verified_at = "2026-07-15"
stale_after_days = 30
affected_count = 6
incident_id = "incident_wat_roundtrip"
blocker_category = "target-contract"
blocker_scope = "full"
blocker_owner = "Wasm backend"
blocker_issue = "#809"
first_seen_commit = "a80b4181"
platforms = ["linux"]
note = "The wasm2wat/wat2wasm roundtrip gate fails"

[[checks]]
id = "check_component_interop_wasmtime"
guarantee_id = "emit_component"
command = "python3 scripts/manager.py verify component-interop"
ci_job = "verification"
release_blocking = true
included_in_full = true
included_in_quick = false
result = "fail"
evidence_type = "fixture-set"
verification_environment = "linux; repository verification toolchain"
last_verified_commit = "2cd10f16"
verified_at = "2026-07-15"
stale_after_days = 30
affected_count = 103
incident_id = "incident_component_interop_103"
blocker_category = "component-interop"
blocker_scope = "full"
blocker_owner = "component model"
blocker_issue = "#810"
first_seen_commit = "a80b4181"
platforms = ["linux"]
note = "All wasmtime component-interop cases fail. Dedicated command (not aggregate verify full)."

[[checks]]
id = "check_opt_equivalence"
guarantee_id = ""
command = "bash scripts/run/test-opt-equivalence.sh --quick"
ci_job = "verification"
release_blocking = false
included_in_full = false
included_in_quick = true
result = "pass"
evidence_type = "smoke"
verification_environment = "linux; repository verification toolchain"
last_verified_commit = "a80b4181"
verified_at = "2026-07-11"
stale_after_days = 30
platforms = ["linux"]
note = "O0 == O1 equivalence"

[[checks]]
id = "check_binary_version"
guarantee_id = ""
command = "arukellt --version"
ci_job = "verification"
release_blocking = false
included_in_full = false
included_in_quick = false
result = "pass"
evidence_type = "smoke"
verification_environment = "linux; repository verification toolchain"
last_verified_commit = "a80b4181"
verified_at = "2026-07-11"
stale_after_days = 30
platforms = ["linux"]
note = "Binary smoke: --version exits 0"

# ── Non-blocking provisional checks ───────────────────────────────────────────

[[checks]]
id = "check_emit_component"
guarantee_id = "emit_component"
command = "python3 scripts/check/gate-666-component-library-emit.py"
ci_job = "verification"
release_blocking = false
included_in_full = true
included_in_quick = false
result = "partial"
evidence_type = "smoke"
verification_environment = "linux; repository verification toolchain"
last_verified_commit = "a80b4181"
verified_at = "2026-07-11"
stale_after_days = 30
platforms = ["linux"]

[[checks]]
id = "check_lsp"
guarantee_id = "lsp"
command = "python3 scripts/manager.py verify quick"
ci_job = "verification"
release_blocking = false
included_in_full = false
included_in_quick = true
result = "pass"
evidence_type = "smoke"
verification_environment = "linux; repository verification toolchain"
last_verified_commit = "a80b4181"
verified_at = "2026-07-11"
stale_after_days = 30
platforms = ["linux"]
note = "LSP gates within verify quick"

[[checks]]
id = "check_ark_toml"
guarantee_id = "ark_toml"
command = "arukellt build"
ci_job = "verification"
release_blocking = false
included_in_full = false
included_in_quick = false
result = "not-run"
evidence_type = "smoke"
verification_environment = "linux; repository verification toolchain"
last_verified_commit = "a80b4181"
platforms = ["linux"]

[[checks]]
id = "check_dap"
guarantee_id = "dap"
command = "manual / scaffold"
ci_job = "none"
release_blocking = false
included_in_full = false
included_in_quick = false
result = "not-run"
evidence_type = "manual"
verification_environment = "linux; repository verification toolchain"
last_verified_commit = ""
platforms = ["linux"]

[[checks]]
id = "check_vscode_dap"
guarantee_id = "vscode_dap"
command = "extension-tests (partial)"
ci_job = "extension-tests"
release_blocking = false
included_in_full = false
included_in_quick = false
result = "partial"
evidence_type = "smoke"
verification_environment = "linux; repository verification toolchain"
last_verified_commit = "a80b4181"
verified_at = "2026-07-11"
stale_after_days = 30
platforms = ["linux"]
