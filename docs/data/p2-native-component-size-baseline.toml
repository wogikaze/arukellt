# Baseline for scripts/check/gate-668-p2-size.py (#668).
# Measured on wasi_p2_native/hello.ark --emit component (guest-native ~4KB).
# Ceiling leaves headroom for small emit churn. The gate checks the official
# component contract and rejects legacy P1/bridge markers separately.

fixture = "tests/fixtures/wasi_p2_native/hello.ark"
max_bytes = 8192
