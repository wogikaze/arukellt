# std::io family

> This file is generated by `python3 scripts/generate-docs.py` from source doc comments and [`../../../std/manifest.toml`](../../../std/manifest.toml).
Source-backed docs for terminal I/O, filesystem, path, process, environment, and CLI helpers.

## `std::io`

- Source: [`../../../std/io/mod.ark`](../../../std/io/mod.ark)
- Manifest-backed functions: 4
- Stability: stable 4

Terminal I/O helpers backed by the current print intrinsics.

Reader and writer abstractions are still deferred. The current module only
exposes direct stdout and stderr helpers.

### Public API

| Name | Signature | Stability | Summary |
|------|-----------|-----------|---------|
| `write_stdout` | `(String) -> ()` | `stable` | Writes a string to stdout without appending a newline. |
| `write_stderr` | `(String) -> ()` | `stable` | Writes a string to stderr without appending a newline. |
| `writeln_stdout` | `(String) -> ()` | `stable` | Writes a string to stdout and appends a newline. |
| `writeln_stderr` | `(String) -> ()` | `stable` | Writes a string to stderr and appends a newline. |

## `std::fs`

- Source: [`../../../std/fs/mod.ark`](../../../std/fs/mod.ark)
- Manifest-backed functions: 2
- Stability: stable 2

Filesystem operations backed by the current WASI P1 intrinsics.

The current surface is intentionally small: read a file into a string or
write a string to a file.

### Public API

| Name | Signature | Stability | Summary |
|------|-----------|-----------|---------|
| `read_to_string` | `(String) -> String` | `stable` | Reads a UTF-8 text file into memory. |
| `write_string` | `(String, String) -> ()` | `stable` | Writes a UTF-8 string to a file, replacing any existing contents. |

## `std::path`

- Source: [`../../../std/path/mod.ark`](../../../std/path/mod.ark)
- Manifest-backed functions: 6
- Stability: stable 6

Pure string-based path manipulation helpers.

Paths are represented as `String` values and always use `/` as the
separator to match POSIX and WASI conventions.

### Public API

| Name | Signature | Stability | Summary |
|------|-----------|-----------|---------|
| `join` | `(String, String) -> String` | `stable` | Joins two path segments with a single / separator. |
| `file_name` | `(String) -> String` | `stable` | Returns the final path segment after the last /. |
| `extension` | `(String) -> String` | `stable` | Returns the extension without the leading .. |
| `with_extension` | `(String, String) -> String` | `stable` | Replaces the current extension, or appends one when missing. |
| `is_absolute` | `(String) -> bool` | `stable` | Returns true when the path starts with /. |
| `parent` | `(String) -> String` | `stable` | Returns the parent directory path, or an empty string at the root. |

## `std::process`

- Source: [`../../../std/process/mod.ark`](../../../std/process/mod.ark)
- Manifest-backed functions: 2
- Stability: stable 2

Process-control helpers.

The current implementation uses lightweight fallbacks because the WASI
`proc_exit` import is not wired in yet.

### Public API

| Name | Signature | Stability | Summary |
|------|-----------|-----------|---------|
| `exit` | `(i32) -> ()` | `stable` | Requests process termination with the given exit code. |
| `abort` | `() -> ()` | `stable` | Aborts execution immediately by panicking. |

## `std::env`

- Source: [`../../../std/env/mod.ark`](../../../std/env/mod.ark)
- Manifest-backed functions: 3
- Stability: stable 3

Runtime environment helpers.

Until the WASI argument and environment imports are wired in, these
functions expose stubbed fallback behavior.

### Public API

| Name | Signature | Stability | Summary |
|------|-----------|-----------|---------|
| `args` | `() -> Vec<String>` | `stable` | Returns the process argument vector. |
| `var` | `(String) -> Option<String>` | `stable` | Looks up an environment variable by name. |
| `arg_count` | `() -> i32` | `stable` | Returns the number of process arguments. |

## `std::cli`

- Source: [`../../../std/cli/mod.ark`](../../../std/cli/mod.ark)
- Manifest-backed functions: 3
- Stability: stable 3

Command-line argument helpers layered on top of `std::env`.

The current implementation is a placeholder until WASI argument imports are
wired into the runtime.

### Public API

| Name | Signature | Stability | Summary |
|------|-----------|-----------|---------|
| `arg_at` | `(i32) -> Option<String>` | `stable` | - |
| `has_flag` | `(String) -> bool` | `stable` | - |
| `cli_arg_count` | `() -> i32` | `stable` | - |
