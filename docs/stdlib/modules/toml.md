# std::toml

> This file is generated by `python3 scripts/generate-docs.py` from source doc comments and [`../../../std/manifest.toml`](../../../std/manifest.toml).
Source-backed docs for the current TOML helpers.

## Overview

> **Overview vs Reference:** This section is curated prose — it explains when and how to use this module family. The sections below are exhaustive generated reference tables sourced directly from `std/manifest.toml` and source doc comments.

The `std::toml` module provides minimal experimental TOML helpers. The current implementation handles only simple `key=value` style lines; full TOML table and array support is not yet available.

**Recommended API highlights:**

| API | Purpose |
|-----|---------|
| `toml_parse_line(line)` | Parse a `key=value` line and return the value as a string. |

**Target constraints:** All targets. No host capability required.

**Typical usage:**

```ark
import std::toml

let value = toml_parse_line("name = \"arukellt\"")
// value == "arukellt"
```

---


## `std::toml`

- Source: [`../../../std/toml/mod.ark`](../../../std/toml/mod.ark)
- Manifest-backed functions: 1
- Stability: experimental 1

Experimental TOML helpers.

The current implementation only handles simple `key=value` style lines.

### Public API

| Name | Signature | Stability | Summary |
|------|-----------|-----------|---------|
| `toml_parse_line` | `(String) -> String` | `experimental` | - |
