# std::wasm

> This file is generated by `python3 scripts/generate-docs.py` from source doc comments and [`../../../std/manifest.toml`](../../../std/manifest.toml).
Source-backed docs for WebAssembly helpers.

## Overview

> **Overview vs Reference:** This section is curated prose — it explains when and how to use this module family. The sections below are exhaustive generated reference tables sourced directly from `std/manifest.toml` and source doc comments.

The `std::wasm` module exposes WebAssembly binary-format constants and low-level memory operations. It provides the magic bytes, version bytes, section IDs, value-type constants, and bulk memory helpers (`memory_copy`, `memory_fill`) needed to build or inspect Wasm modules programmatically.

**Recommended API highlights:**

| API | Purpose |
|-----|---------|
| `wasm_magic()` / `wasm_version()` | The 4-byte Wasm magic number and version bytes. |
| `section_type()` … `section_data()` | Section ID constants for all standard Wasm sections. |
| `valtype_i32()` … `valtype_f64()` | Value-type constants matching the Wasm binary encoding. |
| `memory_copy(dst, src, len)` | Bulk memory copy (like `memory.copy`). |
| `memory_fill(dst, val, len)` | Bulk memory fill (like `memory.fill`). |

**Target constraints:** All targets. No host capability required.

**Typical usage:**

```ark
import std::wasm

let magic = wasm_magic()    // [0x00, 0x61, 0x73, 0x6d]
let ver   = wasm_version()  // [0x01, 0x00, 0x00, 0x00]
let type_section = section_type()  // 1
let i32_type = valtype_i32()       // 0x7f
```

---


## `std::wasm`

- Source: [`../../../std/wasm/mod.ark`](../../../std/wasm/mod.ark)
- Manifest-backed functions: 19
- Stability: experimental 19

Experimental WebAssembly helpers.

The current surface exposes magic bytes, version bytes, section IDs, and
value-type constants.

### Public API

| Name | Signature | Stability | Summary |
|------|-----------|-----------|---------|
| `wasm_magic` | `() -> Vec<i32>` | `experimental` | - |
| `wasm_version` | `() -> Vec<i32>` | `experimental` | - |
| `section_type` | `() -> i32` | `experimental` | - |
| `section_import` | `() -> i32` | `experimental` | - |
| `section_function` | `() -> i32` | `experimental` | - |
| `section_table` | `() -> i32` | `experimental` | - |
| `section_memory` | `() -> i32` | `experimental` | - |
| `section_global` | `() -> i32` | `experimental` | - |
| `section_export` | `() -> i32` | `experimental` | - |
| `section_start` | `() -> i32` | `experimental` | - |
| `section_element` | `() -> i32` | `experimental` | - |
| `section_code` | `() -> i32` | `experimental` | - |
| `section_data` | `() -> i32` | `experimental` | - |
| `valtype_i32` | `() -> i32` | `experimental` | - |
| `valtype_i64` | `() -> i32` | `experimental` | - |
| `valtype_f32` | `() -> i32` | `experimental` | - |
| `valtype_f64` | `() -> i32` | `experimental` | - |
| `memory_copy` | `(i32, i32, i32) -> ()` | `experimental` | - |
| `memory_fill` | `(i32, i32, i32) -> ()` | `experimental` | - |
