import console

fn power(base: i64, exp: i64) -> i64:
  if exp == 0:
    1
  else:
    base * power(base, exp - 1)

fn power_of_two(exp: i64) -> i64:
  power(2, exp)

fn main():
  (0..=10)
    .map(power_of_two)
    .map(string)
    .join(", ")
    |> console.println
