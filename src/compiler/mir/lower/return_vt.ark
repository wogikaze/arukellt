use corehir::fn_info
use mir::value_types

fn mir_fn_info_return_vt(fn_infos: Vec<CoreHirFnInfo>, info_index: i32, fallback: i32) -> i32 {
    if info_index < 0 || info_index >= len(fn_infos) {
        return fallback
    }
    value_types::VT_I32()
}
