use mir::function_core_record
use mir::source_location_record
use mir::types_function

fn MirFunction_name(f: MirFunction) -> String {
    clone(f.name)
}

fn MirFunction_source_location(f: MirFunction) -> SourceLocation {
    f.source_location
}
