// Arukellt Resolver - higher-order sequence builtin symbol names.
// All HOF functions are now auto-imported from std/prelude.ark.
// This file is intentionally empty — no builtins remain in this category.

fn push_hof_builtin_symbol_names(names: Vec<String>) {
}
