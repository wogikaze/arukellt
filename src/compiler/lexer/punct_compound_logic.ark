use lexer::tokens
use lexer::cursor
use lexer::punct_compound_peek

fn lex_logical_punct(lex: Lexer, start: i32, c: i32) -> Token {
    if c == 38 {
        if punct_compound_peek::punct_peek(lex) == 38 {
            cursor::advance_by(lex, 1)
            return Token_new(tokens::TK_AMPAMP(), "&&", start, cursor::lexer_position(lex))
        }
        return Token_new(tokens::TK_AMP(), "&", start, cursor::lexer_position(lex))
    }
    if punct_compound_peek::punct_peek(lex) == 124 {
        cursor::advance_by(lex, 1)
        return Token_new(tokens::TK_PIPEPIPE(), "||", start, cursor::lexer_position(lex))
    }
    Token_new(tokens::TK_PIPE(), "|", start, cursor::lexer_position(lex))
}
