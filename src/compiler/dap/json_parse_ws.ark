// Arukellt Selfhost - DAP JSON whitespace scanning

fn dap_skip_ws(s: String, pos: i32) -> i32 {
	let n = len(s)
	let mut p = pos
	while p < n {
		let c = char_at(s, p)
		if c != 32 && c != 9 && c != 10 && c != 13 {
			return p
		}
		p = p + 1
	}
	p
}
