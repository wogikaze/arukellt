use parser::kinds

fn expr_operator_node_kind_name(kind: i32) -> String {
    if kind == kinds::NK_BINARY() {
        return "Binary"
    }
    if kind == kinds::NK_UNARY() {
        return "Unary"
    }
    if kind == kinds::NK_ASSIGN() {
        return "Assign"
    }
    if kind == kinds::NK_RANGE() {
        return "Range"
    }
    String_new()
}
