use parser::kinds

fn expr_aggregate_node_kind_name(kind: i32) -> String {
    if kind == kinds::NK_STRUCT_LIT() {
        return "StructLit"
    }
    if kind == kinds::NK_ARRAY_LIT() {
        return "ArrayLit"
    }
    if kind == kinds::NK_TUPLE() {
        return "Tuple"
    }
    if kind == kinds::NK_COMPREHENSION() {
        return "Comprehension"
    }
    String_new()
}
