use parser::kinds

fn expr_control_node_kind_name(kind: i32) -> String {
    if kind == kinds::NK_IF_EXPR() {
        return "If"
    }
    if kind == kinds::NK_MATCH_EXPR() {
        return "Match"
    }
    if kind == kinds::NK_CLOSURE() {
        return "Closure"
    }
    if kind == kinds::NK_BLOCK() {
        return "Block"
    }
    String_new()
}
