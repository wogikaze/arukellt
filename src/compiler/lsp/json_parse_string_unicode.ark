// Arukellt Selfhost - LSP JSON unicode escape decoding.
//
// Delegates to std::json (issue #704).

use std::json

fn json_escape_unicode_ascii(s: String, pos: i32) -> String {
    json::json_decode_unicode_ascii(s, pos)
}
