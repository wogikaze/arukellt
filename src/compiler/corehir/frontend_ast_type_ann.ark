// Arukellt Selfhost - CoreHIR frontend AST type annotation accessors.

use parser::ast_node_record

fn corehir_node_type_ann_count(node: AstNode) -> i32 {
    len(node.type_ann)
}

fn corehir_node_type_ann_at(node: AstNode, idx: i32) -> AstNode {
    get_unchecked(node.type_ann, idx)
}
