// Arukellt Selfhost - CoreHIR raw program body views.

use corehir::raw_record

fn program_body_table(program: CoreHirRawProgram) -> CoreHirBodyTable {
    raw_record::raw_program_body_table(program)
}
