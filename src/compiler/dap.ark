use dap::framing
use dap::session
use dap::state_record

/// Processes a complete DAP byte stream and returns all framed protocol responses.
pub fn run_dap_session(input: String) -> String {
    session::run_dap_session(input)
}
