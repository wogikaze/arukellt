use typechecker::kinds
// Arukellt Typechecker - type annotation node predicates.

use typechecker::ast_access

fn node_is_type_generic(node: AstNode) -> bool {
    ast_access::node_kind_is(node, kinds::NK_TYPE_GENERIC())
}
