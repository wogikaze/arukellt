use typechecker::entry

pub fn typecheck_module(decls: Vec<AstNode>, resolve_ctx: ResolveCtx, wit_paths: Vec<String>) -> TypeCheckResult {
    entry::typecheck_module(decls, resolve_ctx, wit_paths)
}
