use component

/// Preserves the component-emitter boundary while delegating to the canonical component pipeline.
pub fn emit_component(core_wasm: Vec<i32>, mir: MirModule, target: String, wasi_version: String, world: String) -> Vec<i32> {
    component::emit_component(core_wasm, mir, target, wasi_version, world)
}
