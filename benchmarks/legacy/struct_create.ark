struct Point {
    x: i32,
    y: i32,
}

fn make_point(a: i32, b: i32) -> Point {
    Point { x: a, y: b }
}

fn main() {
    let p = make_point(100, 200)
    println((p.x).to_string())
    println((p.y).to_string())
}
