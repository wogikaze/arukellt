use std::host::stdio

fn main() {
    let mut s: String = String_from("")
    let mut i: i32 = 0
    while i < 100 {
        s = concat(s, String_from("x"))
        i = i + 1
    }
    stdio::println((len(s)).to_string())
}
