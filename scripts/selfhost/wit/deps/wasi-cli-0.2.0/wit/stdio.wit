interface stdin {
  read: func(a: u32, b: u32, c: u32, d: u32) -> u32;
}

interface stdout {
  write: func(a: u32, b: u32, c: u32, d: u32) -> u32;
}

interface stderr {
  write-err: func(a: u32, b: u32, c: u32, d: u32) -> u32;
}
