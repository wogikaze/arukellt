// Export a small math API as a Wasm component.
// Hosts (JS, Wasmtime CLI, or another Ark component) call these via the Component Model.
pub fn add(a: i32, b: i32) -> i32 {
    a + b
}

pub fn mul(a: i32, b: i32) -> i32 {
    a * b
}

fn main() {}
