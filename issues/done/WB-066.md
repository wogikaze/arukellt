# WB-066 Remove the legacy `WORKBOARD.md` redirect and rename queue guardrails to `issues`

```json
{
  "id": "WB-066",
  "title": "Remove the legacy `WORKBOARD.md` redirect and rename queue guardrails to `issues`",
  "status": "done",
  "priority": "p3",
  "area": ["tooling", "docs", "issues"],
  "depends_on": ["WB-064"],
  "blocked_on": [],
  "source": "user request: delete `WORKBOARD.md` if the repository is fully migrated to `issues/`",
  "created_at": "2026-03-22",
  "updated_at": "2026-03-22"
}
```

## Done When

- `WORKBOARD.md` no longer exists and the repository guardrails assert the `issues/` layout directly
- `./scripts/verify-harness.sh` and the harness docs use `cargo test -p arktc --test issues`
- queue and harness docs describe the guardrail as issues queue validation instead of workboard validation

## Notes

- 2026-03-22: Removed the legacy `WORKBOARD.md` redirect stub, renamed `crates/arktc/tests/workboard.rs` to `crates/arktc/tests/issues.rs`, and updated `scripts/verify-harness.sh`, `AGENTS.md`, `README.md`, and the harness ADR/docs to describe issues queue validation directly. Verified with `cargo fmt`, `cargo test -p arktc --test issues`, and `cargo test`.
- This intentionally revises the earlier migration contract from `WB-064`/`WB-065`: the repository no longer keeps a compatibility redirect for the old monolithic workboard file.
