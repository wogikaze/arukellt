# WB-065 Make the workspace clippy-clean enough to gate the harness

```json
{
  "id": "WB-065",
  "title": "Make the workspace clippy-clean enough to gate the harness",
  "status": "done",
  "priority": "p3",
  "area": ["tooling", "lang-core", "workboard"],
  "depends_on": ["WB-064"],
  "blocked_on": [],
  "source": "follow-up after `WB-064`: workspace `Cargo.toml` now declares stricter clippy policy, but `cargo clippy` still fails on pre-existing warnings and widespread test `expect()` usage",
  "created_at": "2026-03-21",
  "updated_at": "2026-03-21"
}
```

## Done When

- `cargo clippy --workspace --lib --bins -- -D warnings` passes with the workspace clippy policy from `Cargo.toml`
- the remaining decision on whether test targets must also satisfy `expect_used` / `unwrap_used` is encoded in code or queue state rather than left implicit
- `./scripts/verify-harness.sh` can run a clippy guardrail without immediately failing on known pre-existing violations

## Notes

- 2026-03-21: Enabled workspace lint inheritance across all crates, cleaned the concrete `lang-core` and production-code clippy blockers that prevented gating, and scoped the enforced clippy policy to `cargo clippy --workspace --lib --bins -- -D warnings` with explicit allowlist decisions for noisy style-only pedantic lints. Updated `scripts/verify-harness.sh`, pointer docs, and `crates/arktc/tests/workboard.rs` so the failing clippy gate is part of the harness contract. Verified with `cargo fmt`, `cargo clippy --workspace --lib --bins -- -D warnings`, `./scripts/verify-harness.sh`, `cargo test -p arktc --test workboard`, and `cargo test`.
- Split from `WB-064` after adding `[workspace.lints.clippy]` entries for `pedantic`, `unwrap_used`, `expect_used`, `allow_attributes`, and `dbg_macro`
- Current blocking findings are concrete, not theoretical: `lang-core` has `collapsible_if`, `too_many_arguments`, and `if_same_then_else` warnings under `-D warnings`, while test targets contain widespread `expect()` usage that conflicts with a blanket `expect_used = "deny"` rollout
