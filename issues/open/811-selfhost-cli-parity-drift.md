---
Status: open
Created: 2026-07-14
Updated: 2026-07-14
ID: 811
Track: selfhost
Depends on: none
Orchestration class: ready
Orchestration upstream: None
Blocks v{N}: none
Priority: 3
Source: CQ-18 audit — unresolved verify full failures need open owner
---

# 811 — Selfhost CLI parity drift

## Summary

`verify full` reports 2 selfhost CLI parity failures. The selfhost
compiler's CLI output differs from the reference for 2 commands.

## Exact failure scope

2 CLI commands produce different output between the selfhost compiler and
the reference. The specific commands are captured in the verify full
receipt.

## Machine-readable baseline

The failing CLI command IDs are captured in the verify full receipt
(`docs/data/verify-full-receipt.json`) under `checks[]` where
`check_id == "cli_parity"` and `items[]` where `result == "fail"`.

The receipt is generated by `scripts/gen/write-verify-receipt.py` from
`python3 scripts/manager.py verify full` output.

## Owner

compiler team (CLI)

## Removal condition

Each CLI command passes when the selfhost compiler produces output
matching the reference for that command.

## Validation command

```bash
python3 scripts/manager.py selfhost cli-parity
```

## Current count

2 mismatched commands

## New-failure ratchet

No new CLI parity mismatches may be added. The count must only decrease.
