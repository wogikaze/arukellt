// Component interop smoke test: string parameter canonical ABI with i64 result.

pub fn count64_text(text: String) -> i64 {
    if len(text) == 0 {
        return 0i64
    }
    5000000000i64
}

fn main() {
}
