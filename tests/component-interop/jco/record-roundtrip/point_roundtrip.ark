// Component interop smoke test: Point record parameter and result canonical ABI.

struct Point {
    x: i32,
    y: i32,
}

pub fn shift_point(p: Point) -> Point {
    Point { x: p.x + 1, y: p.y - 1 }
}

fn main() {
}
