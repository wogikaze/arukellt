// Component interop smoke test: Point record parameters and result canonical ABI.

struct Point {
    x: i32,
    y: i32,
}

pub fn add_points(a: Point, b: Point) -> Point {
    Point { x: a.x + b.x, y: a.y + b.y }
}

fn main() {
}
