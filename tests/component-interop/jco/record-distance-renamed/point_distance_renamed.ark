// Component interop smoke test: single record parameter canonical ABI is name-independent.
struct Point {
    x: i32,
    y: i32,
}

pub fn length_sq(p: Point) -> i32 {
    p.x * p.x + p.y * p.y
}

fn main() {
}
