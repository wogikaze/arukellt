# Python host -> Arukellt component fixture

`run.sh` uses the Python `wasmtime` package when installed to instantiate the component path. The fixture is toolchain-optional, matching other external-language interop lanes.
