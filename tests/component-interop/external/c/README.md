# C component provider fixture

`run.sh` validates a C/WASI component supplied in `ARUKELLT_C_COMPONENT`; CI may inject a wasi-sdk/wit-bindgen artifact without making that toolchain a compiler dependency.
