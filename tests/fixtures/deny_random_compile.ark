// Fixture: --deny-random blocks programs that use random intrinsics at compile time.
// Expected: compile-time error from MIR capability scan.
use std::host::stdio
use std::host::random

fn main() {
    let r = random::random_i32()
    stdio::println(i32_to_string(r))
}
