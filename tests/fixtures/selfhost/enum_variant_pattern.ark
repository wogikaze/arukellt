// Test: enum variant pattern in regular match (no payload, no guard)
use std::host::stdio

enum Level { Low, Medium, High }

fn main() {
    let x = Level::Low
    let result = match x {
        Level::Low => "low",
        _ => "other",
    }
    stdio::println(result)
}
