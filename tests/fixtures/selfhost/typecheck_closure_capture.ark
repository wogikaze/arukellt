// Issue #499 (slice): closure body references an outer `let` binding; must typecheck.
use std::host::stdio

fn main() {
    let x = 1 + 2
    let _f = || x + 1
    stdio::println(String_from("ok"))
}
