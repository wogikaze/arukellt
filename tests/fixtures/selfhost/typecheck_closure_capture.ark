// Issue #499: closure captures outer `let`; call site exercises MIR lowering + wasm.
use std::host::stdio

fn main() {
    let x = 1 + 2
    let f = || x + 1
    let y = f()
    stdio::println(to_string(y))
}
