use std::host::stdio
import helpers

fn main() {
    let s: String = helpers::greet(String_from("world"))
    stdio::println(s)
    let v: Vec<i32> = helpers::make_vec()
    stdio::println(i32_to_string(len(v)))
}
