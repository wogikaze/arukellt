use std::host::stdio

fn main() {
    let r: Result<i32, String> = Err(String_from("something went wrong"))
    match r {
        Result::Ok(val) => stdio::println((val).to_string()),
        Result::Err(e) => stdio::println(e),
    }
}
