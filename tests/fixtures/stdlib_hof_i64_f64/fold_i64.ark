fn add(acc: i64, x: i64) -> i64 {
    acc + x
}

fn main() {
    let mut v: Vec<i64> = Vec_new_i64()
    push(v, 10)
    push(v, 20)
    push(v, 30)
    let total: i64 = fold_i64_i64(v, 0, add)
    println(i64_to_string(total))
}
