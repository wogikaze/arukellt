use std::test

fn main() {
    let mut s = String_from("")
    let mut i = 0
    while i < 20 {
        s = concat(s, String_from("ab"))
        i = i + 1
    }
    test::assert_eq_i32(len(s), 40)
    test::assert_true(starts_with(s, String_from("abab")))
}
