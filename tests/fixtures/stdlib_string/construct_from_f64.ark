use std::test

fn main() {
    test::assert_eq_string((3.14).to_string(), "3.14")
    test::assert_eq_string((0.0).to_string(), "0")
    test::assert_eq_string((-2.5).to_string(), "-2.5")
    test::assert_eq_string((42.0).to_string(), "42")
}
