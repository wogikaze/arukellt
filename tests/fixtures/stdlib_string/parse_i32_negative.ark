use std::test

fn main() {
    let r: Result<i32, String> = parse_i32(String_from("-42"))
    match r {
        Result::Ok(v) => test::assert_eq_i32(v, -42),
        Result::Err(e) => panic("expected Ok"),
    }
}
