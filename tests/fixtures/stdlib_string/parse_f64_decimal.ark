use std::test

fn main() {
    let r: Result<f64, String> = parse_f64(String_from("3.14159"))
    match r {
        Result::Ok(v) => test::assert_eq_f64(v, 3.14159),
        Result::Err(e) => panic("expected Ok"),
    }
}
