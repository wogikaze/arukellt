use std::test

fn main() {
    let r: Result<f64, String> = parse_f64(String_from("not-a-number"))
    match r {
        Result::Ok(v) => panic("expected Err"),
        Result::Err(e) => test::assert_true(true),
    }
}
