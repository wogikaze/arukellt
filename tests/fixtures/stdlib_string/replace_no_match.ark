use std::test

fn main() {
    let s = String_from("hello")
    test::assert_eq_string(replace(s, String_from("zz"), String_from("QQ")), "hello")
    test::assert_eq_string(replace(s, String_from(""), String_from("Q")), "hello")
}
