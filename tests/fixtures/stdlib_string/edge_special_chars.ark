use std::test

fn main() {
    let s = String_from("a.b,c!d@e")
    test::assert_eq_i32(len(s), 9)
    test::assert_true(contains(s, String_from("!")))
    test::assert_true(contains(s, String_from("@")))
}
