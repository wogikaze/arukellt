use std::test

fn main() {
    let r: Result<f64, String> = parse_f64(String_from("-0.5"))
    match r {
        Result::Ok(v) => test::assert_eq_f64(v, -0.5),
        Result::Err(e) => panic("expected Ok"),
    }
}
