use std::test

fn main() {
    test::assert_true(eq(String_from(""), String_from("")))
    test::assert_false(eq(String_from(""), String_from("a")))
    test::assert_false(eq(String_from("a"), String_from("")))
}
