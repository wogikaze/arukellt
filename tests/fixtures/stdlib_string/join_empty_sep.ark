use std::test

fn main() {
    let parts: Vec<String> = Vec_new_String()
    push(parts, String_from("a"))
    push(parts, String_from("b"))
    push(parts, String_from("c"))
    test::assert_eq_string(join(parts, String_from("")), "abc")
}
