use std::test

fn main() {
    let s = String_from("hello")
    test::assert_true(ends_with(s, String_from("")))
    test::assert_false(ends_with(String_from(""), String_from("x")))
    test::assert_true(ends_with(String_from(""), String_from("")))
}
