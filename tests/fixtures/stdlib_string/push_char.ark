use std::test

fn main() {
    let mut s = String_from("hello")
    push_char(s, '!')
    let mut expected = String_from("hello")
    push_char(expected, '!')
    test::assert_true(eq(s, expected))
}
