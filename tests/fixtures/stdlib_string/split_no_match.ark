use std::test

fn main() {
    let s = String_from("hello")
    let parts = split(s, String_from(","))
    test::assert_eq_i32(len(parts), 1)
    test::assert_eq_string(get_unchecked(parts, 0), "hello")
}
