use std::test

fn main() {
    let s = repeat(String_from("a."), 30)
    let r = replace(s, String_from("."), String_from("-"))
    test::assert_eq_i32(len(r), 60)
    test::assert_false(contains(r, String_from(".")))
}
