use std::host::stdio
use std::text

fn main() {
    let s1 = String_from("hello")
    let s2 = String_from(" world")
    let s3 = text::concat(s1, s2)
    stdio::println(s3)

    let sub = slice(s3, 0, 5)
    stdio::println(sub)
}
