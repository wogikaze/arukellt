use std::host::stdio

fn countdown(n: i32) -> i32 {
    if n <= 0 {
        0
    } else {
        countdown(n - 1)
    }
}

fn main() {
    let result = countdown(10)
    stdio::println((result).to_string())
}
