use std::core::math
use std::test

fn main() {
    // Any base to the power of 0 is 1
    test::assert_eq_i32(2.pow(0), 1)
    test::assert_eq_i32(0.pow(0), 1)
    test::assert_eq_i32(100.pow(0), 1)
    test::assert_eq_i32((-5).pow(0), 1)
}
