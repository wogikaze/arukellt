use std::core::cmp
use std::test

fn main() {
    // Value above range is clamped to hi
    test::assert_eq_i32(15.clamp(0, 10), 10)
    test::assert_eq_i32(100.clamp(0, 10), 10)
    test::assert_eq_i32(11.clamp(0, 10), 10)
}
