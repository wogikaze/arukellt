use std::core::convert
use std::test

fn main() {
    test::assert_eq_string(9001i64.to_string(), "9001")
    test::assert_eq_string(0i64.to_string(), "0")
    test::assert_eq_string((-1i64).to_string(), "-1")
}
