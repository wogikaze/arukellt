use std::core::cmp
use std::test

fn main() {
    // Value within range is unchanged
    test::assert_eq_i32(5.clamp(0, 10), 5)
    test::assert_eq_i32(0.clamp(0, 10), 0)
    test::assert_eq_i32(10.clamp(0, 10), 10)
    test::assert_eq_i32(3.clamp(0, 10), 3)
}
