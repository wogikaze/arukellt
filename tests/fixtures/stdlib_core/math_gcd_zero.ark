use std::core::math
use std::test

fn main() {
    // gcd(x, 0) = x, gcd(0, 0) = 0
    test::assert_eq_i32(math::gcd(5, 0), 5)
    test::assert_eq_i32(math::gcd(0, 5), 5)
    test::assert_eq_i32(math::gcd(0, 0), 0)
}
