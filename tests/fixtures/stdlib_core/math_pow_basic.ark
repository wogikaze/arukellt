use std::core::math
use std::test

fn main() {
    test::assert_eq_i32(2.pow(10), 1024)
    test::assert_eq_i32(3.pow(3), 27)
    test::assert_eq_i32(5.pow(2), 25)
    test::assert_eq_i32(10.pow(3), 1000)
}
