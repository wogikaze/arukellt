use std::core::hash
use std::test

fn main() {
    let strings = ["abc", "hello world", "12345", "A", ""]
    let mut i = 0
    while i < 5 {
        let s = get_unchecked(strings, i)
        test::assert_eq_i32(hash::hash_string(s), hash::hash_string(s))
        i = i + 1
    }
}
