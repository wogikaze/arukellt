use std::core::math
use std::test

fn main() {
    test::assert_eq_f64(math::sqrt(0.0), 0.0)
    test::assert_eq_f64(math::sqrt(1.0), 1.0)
    test::assert_eq_f64(math::sqrt(4.0), 2.0)
    test::assert_eq_f64(math::sqrt(9.0), 3.0)
    test::assert_eq_f64(math::sqrt(16.0), 4.0)
}
