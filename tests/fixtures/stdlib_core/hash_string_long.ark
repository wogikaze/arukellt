use std::core::hash
use std::test

fn main() {
    let s = "abcdefghijklmnopqrstuvwxyz0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZ"
    let h1 = hash::hash_string(s)
    let h2 = hash::hash_string(s)
    test::assert_eq_i32(h1, h2)
    test::assert_true(h1 >= 0)
}
