use std::core::cmp
use std::test

fn main() {
    // Value below range is clamped to lo
    test::assert_eq_i32(cmp::clamp(-5, 0, 10), 0)
    test::assert_eq_i32(cmp::clamp(-100, 0, 10), 0)
    test::assert_eq_i32(cmp::clamp(-1, 0, 10), 0)
}
