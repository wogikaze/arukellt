use std::core::cmp
use std::test

fn main() {
    test::assert_eq_i32(10.max(3), 10)
    test::assert_eq_i32(3.max(10), 10)
    test::assert_eq_i32(-5.max(0), 0)
    test::assert_eq_i32(7.max(7), 7)
}
