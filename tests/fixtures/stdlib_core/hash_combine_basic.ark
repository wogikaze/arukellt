use std::core::hash
use std::test

fn main() {
    let h1 = hash::hash_i32(42)
    let h2 = hash::hash_string("hello")
    let combined = hash::combine(h1, h2)
    test::assert_true(combined >= 0)
    // combine is deterministic
    test::assert_eq_i32(combined, hash::combine(h1, h2))
}
