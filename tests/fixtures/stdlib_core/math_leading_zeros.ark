use std::core::math
use std::test

fn main() {
    test::assert_eq_i32(0.leading_zeros(), 32)
    test::assert_eq_i32(1.leading_zeros(), 31)
    test::assert_eq_i32(0x40000000.leading_zeros(), 1)
    test::assert_eq_i32((-1).leading_zeros(), 0)
}
