use std::host::stdio

struct Point {
    x: i32,
    y: i32,
}

fn main() {
    let mut items = Vec_new_Point()
    items = push(items, Point { x: 1, y: 2 })
    let p = get_unchecked(items, 0)
    stdio::println(p.x.to_string())
    stdio::println(p.y.to_string())
}
