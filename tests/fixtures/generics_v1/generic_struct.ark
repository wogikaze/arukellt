use std::host::stdio

struct Pair<T> {
    first: T,
    second: T,
}

fn main() {
    let p: Pair<i32> = Pair { first: 10, second: 20 }
    stdio::println((p.first).to_string())
    stdio::println((p.second).to_string())
}
