use std::host::stdio
struct Pair<T> {
    first: T,
    second: T,
}

fn main() {
    let p: Pair<i32> = Pair { first: 10, second: 20 }
    stdio::println(i32_to_string(p.first))
    stdio::println(i32_to_string(p.second))
}
