// Fixture: --deny-clock blocks programs that use clock intrinsics at compile time.
// Expected: compile-time error from MIR capability scan.
use std::host::clock
use std::host::stdio

fn main() {
    let t = clock::monotonic_now()
    stdio::println(i64_to_string(t))
}
