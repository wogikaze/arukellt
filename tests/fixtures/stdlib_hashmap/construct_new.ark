// Construction: hashmap_new() basic invariants
use std::collections::hash
use std::test

fn main() {
    let m = hash::hashmap_new()
    test::assert_true(hash::hashmap_is_empty(m))
    test::assert_eq_i32(hash::hashmap_size(m), 0)
    test::assert_eq_i32(hash::hashmap_capacity(m), 16)
    test::assert_false(hash::hashmap_contains(m, 0))
    test::assert_false(hash::hashmap_contains(m, 1))
    test::assert_false(hash::hashmap_contains(m, 100))
    test::assert_eq_i32(len(hash::hashmap_keys(m)), 0)
    test::assert_eq_i32(len(hash::hashmap_values(m)), 0)
    test::assert_eq_i32(len(hash::hashmap_entries(m)), 0)
}
