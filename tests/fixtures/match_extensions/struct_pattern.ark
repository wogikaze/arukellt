struct Point {
    x: i32,
    y: i32,
}

fn main() {
    let p = Point { x: 3, y: 4 }
    match p {
        Point { x, y } => {
            println(i32_to_string(x))
            println(i32_to_string(y))
        }
    }
}
