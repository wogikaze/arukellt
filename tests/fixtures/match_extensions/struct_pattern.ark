use std::host::stdio

struct Point {
    x: i32,
    y: i32,
}

fn main() {
    let p = Point { x: 3, y: 4 }
    match p {
        Point { x, y } => {
            stdio::println((x).to_string())
            stdio::println((y).to_string())
        },
    }
}
