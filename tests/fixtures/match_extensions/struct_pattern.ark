use std::host::stdio

struct Point {
    x: i32,
    y: i32,
}

fn main() {
    let p = Point { x: 3, y: 4 }
    match p {
        Point { x, y } => {
            stdio::println(i32_to_string(x))
            stdio::println(i32_to_string(y))
        },
    }
}
