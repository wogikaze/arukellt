use std::host::stdio

struct Point {
    x: i32,
    y: i32,
}

fn main() {
    let p1 = Point { x: 1, y: 2 }
    let p2 = Point { x: 10, ..p1 }
    stdio::println((p2.x).to_string())
    stdio::println((p2.y).to_string())
}
