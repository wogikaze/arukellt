use std::host::stdio

fn add_f32(a: f32, b: f32) -> f32 {
    a + b
}

fn main() {
    let x: f32 = 1.5f32
    let y: f32 = 2.5f32
    let z: f32 = add_f32(x, y)
    stdio::println(f32_to_string(z))
}
