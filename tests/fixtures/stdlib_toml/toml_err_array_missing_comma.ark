use std::host::stdio
use std::toml
use std::test

fn main() {
    let t = toml::toml_parse("vals = [1 2]")
    match t {
        Result::Ok(_) => assert(false),
        Result::Err(_) => {},
    }
    stdio::println("OK")
}
// array elements without comma separator are rejected
