use std::host::stdio
use std::toml
use std::test

fn main() {
    let t = toml::toml_parse("vals = [1, 2")
    match t {
        Result::Ok(_) => assert(false),
        Result::Err(_) => {},
    }
    stdio::println("OK")
}
// unterminated array is rejected
