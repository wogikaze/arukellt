use std::host::stdio
use std::toml
use std::test

fn main() {
    let t = toml::toml_parse("p = { x 1 }")
    match t {
        Result::Ok(_) => assert(false),
        Result::Err(_) => {},
    }
    stdio::println("OK")
}
// inline table field missing = is rejected
