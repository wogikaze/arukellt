use std::host::stdio
use std::toml
use std::test

fn main() {
    let t = toml::toml_parse("[[items\nname = \"x\"")
    match t {
        Result::Ok(_) => assert(false),
        Result::Err(_) => {},
    }
    stdio::println("OK")
}
// unclosed array-of-tables header is rejected
