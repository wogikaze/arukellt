use std::host::stdio
use std::toml
use std::test

fn main() {
    let t = toml::toml_parse("val = 'line\nbreak'")
    match t {
        Result::Ok(_) => assert(false),
        Result::Err(_) => {},
    }
    stdio::println("OK")
}
// literal string containing newline is rejected
