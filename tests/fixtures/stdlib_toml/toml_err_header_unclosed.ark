use std::host::stdio
use std::toml
use std::test

fn main() {
    let t = toml::toml_parse("[server\nhost = \"x\"")
    match t {
        Result::Ok(_) => assert(false),
        Result::Err(_) => {},
    }
    stdio::println("OK")
}
// table header missing closing ] is rejected
