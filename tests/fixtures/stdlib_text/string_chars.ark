use std::text
use std::host::stdio

fn main() {
    // chars returns individual characters (best-effort via split)
    let cs = text::chars("abc")
    stdio::println(i32_to_string(len(cs)))

    // empty string chars
    let empty_cs = text::chars("")
    stdio::println(i32_to_string(len(empty_cs)))
}
