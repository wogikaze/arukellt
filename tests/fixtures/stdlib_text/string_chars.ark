use std::host::stdio
use std::text

fn main() {
    let cs = text::chars("abc")
    stdio::println((len(cs)).to_string())
    let empty_cs = text::chars("")
    stdio::println((len(empty_cs)).to_string())
}
// chars returns individual characters (best-effort via split)
// empty string chars
