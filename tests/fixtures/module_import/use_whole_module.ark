// Fixture: use_whole_module.ark
// Slice 2 of issue #039: verify that `use std::text` (whole-module import)
// resolves correctly, binding all pub items under the `text::` qualifier.
use std::text

fn main() {
    let empty = text::is_empty("")
    if empty {
        __intrinsic_println("yes")
    } else {
        __intrinsic_println("no")
    }
    let n = text::len_bytes("hello")
    __intrinsic_println(__intrinsic_i32_to_string(n))
}
