// Issue #701: Associated function syntax — String::from().
// Verifies String::from("hello") compiles and produces a String.
use std::host::stdio

fn main() {
    let s = String::from("hello")
    assert(eq(s, "hello"))
    stdio::println(s)
}
