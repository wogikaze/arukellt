use std::host::stdio

fn main() {
    let mut i: i32 = 0
    let result: i32 = loop {
        if i >= 5 {
            break i * 10
        }
        i = i + 1
    }
    stdio::println((result).to_string())
}
