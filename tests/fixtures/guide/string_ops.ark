use std::host::stdio
fn main() {
    let s = String_from("hello")
    let t = concat(s, String_from(" world"))
    let upper = to_upper(s)
    let lower = to_lower(s)
    stdio::println(t)
    stdio::println(upper)
    stdio::println(lower)
}
