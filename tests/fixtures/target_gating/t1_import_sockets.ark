// Fixture: T1 target cannot import std::host::sockets
// Expected: E0500 error at compile time (incompatible target)
// Gating: This should be rejected when --target wasm32-wasi-p1 is used
use std::host::sockets

fn main() {
}
