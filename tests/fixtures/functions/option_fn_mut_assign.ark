use std::host::stdio

fn double(n: i32) -> i32 {
    n * 2
}

fn main() {
    let mut result: Option<fn(i32) -> i32> = None
    result = Some(double)
    match result {
        Some(f) => stdio::println((f(5)).to_string()),
        None => stdio::println("missing"),
    }
}
