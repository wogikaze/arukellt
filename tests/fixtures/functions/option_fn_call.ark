use std::host::stdio

fn double(n: i32) -> i32 {
    n * 2
}

fn call_opt(opt: Option<fn(i32) -> i32>, x: i32) -> i32 {
    match opt {
        Some(g) => g(x),
        None => 0,
    }
}

fn main() {
    stdio::println((call_opt(Some(double), 5)).to_string())
}
