use std::host::stdio
struct Point {
    x: i32,
    y: i32,
}

impl Point {
    fn translate(self, dx: i32, dy: i32) {
        self.x = self.x + dx
        self.y = self.y + dy
    }
}

fn main() -> i32 {
    let p = Point { x: 10, y: 20 }
    stdio::println(p.x)
    stdio::println(p.y)
    p.translate(5, 3)
    stdio::println(p.x)
    stdio::println(p.y)
    p.x = 100
    stdio::println(p.x)
    0
}
