use std::fs
use std::test

fn main() {
    match fs::metadata("some_path") {
        Result::Ok(_) => panic("expected error"),
        Result::Err(err) => test::assert_true(contains(err, "not yet supported")),
    }
}
