use std::host::stdio
use std::test

fn main() {
    let r: Result<i32, String> = parse_i32(String_from("abc"))
    match r {
        Result::Ok(_) => panic("expected error"),
        Result::Err(_) => (),
    }
}
