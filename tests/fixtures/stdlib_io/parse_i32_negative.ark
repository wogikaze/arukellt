use std::host::stdio
use std::test

fn main() {
    let r: Result<i32, String> = parse_i32(String_from("-100"))
    match r {
        Result::Ok(val) => test::assert_eq_i32(val, -100),
        Result::Err(e) => panic(e),
    }
}
