use std::host::fs
use std::host::stdio

fn main() {
    let r = fs::read_to_string("test.txt")
    match r {
        Result::Ok(content) => stdio::println(content),
        Result::Err(e) => stdio::println("fs denied"),
    }
}
