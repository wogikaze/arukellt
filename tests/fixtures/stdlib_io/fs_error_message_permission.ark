use std::host::fs
use std::test

fn main() {
    let err: fs::FsError = fs::FsError::PermissionDenied("secret.txt")
    test::assert_eq_string(fs::fs_error_message(err), "permission denied: secret.txt")
}
