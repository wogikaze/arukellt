use std::bytes
use std::io
use std::test

fn main() {
    let w: Vec<i32> = io::stderr()
    let _: Result<i32, io::IoError> = io::writer_write_str(w, "")
    let b: Vec<i32> = io::writer_to_bytes(w)
    test::assert_eq_i32(bytes::bytes_len(b), 0)
}
