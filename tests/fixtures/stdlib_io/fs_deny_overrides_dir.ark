use std::host::fs
use std::host::stdio
fn main() {
    let r: Result<String, String> = fs::read_to_string("test.txt")
    match r {
        Ok(content) => stdio::println(content),
        Err(e) => stdio::println("deny-fs overrides dir"),
    }
}
