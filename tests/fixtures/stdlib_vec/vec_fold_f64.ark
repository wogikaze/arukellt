use std::test

fn add(acc: f64, x: f64) -> f64 {
    acc + x
}

fn main() {
    let mut v: Vec<f64> = Vec_new_f64()
    push(v, 1.5)
    push(v, 2.5)
    push(v, 3.0)
    let result: f64 = fold_f64_f64(v, 0.0, add)
    test::assert_eq_f64(result, 7.0)
}
