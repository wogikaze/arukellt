use std::test

fn mul(acc: i32, x: i32) -> i32 {
    acc * x
}

fn main() {
    let mut v: Vec<i32> = Vec_new_i32()
    push(v, 1)
    push(v, 2)
    push(v, 3)
    push(v, 4)
    let result: i32 = fold_i32_i32(v, 1, mul)
    test::assert_eq_i32(result, 24)
}
