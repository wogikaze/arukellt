use std::host::stdio
fn add(acc: i32, x: i32) -> i32 {
    acc + x
}

fn main() {
    let mut v: Vec<i32> = Vec_new_i32()
    push(v, 1)
    push(v, 2)
    push(v, 3)
    push(v, 4)
    push(v, 5)
    let sum: i32 = fold_i32_i32(v, 0, add)
    stdio::println(i32_to_string(sum))
}
