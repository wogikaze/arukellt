use std::test

fn main() {
    let mut v: Vec<i32> = Vec_new_i32()
    push(v, 10)
    push(v, 20)
    push(v, 30)
    remove_i32(v, 0)
    test::assert_eq_i32(len(v), 2)
    test::assert_eq_i32(get_unchecked(v, 0), 20)
    test::assert_eq_i32(get_unchecked(v, 1), 30)
}
