use std::test

fn main() {
    let mut v: Vec<i32> = Vec::new<i32>()
    match pop(v) {
        Option::Some(val) => panic("expected None, got Some"),
        None => {
        },
    }
    test::assert_eq_i32(len(v), 0)
}
