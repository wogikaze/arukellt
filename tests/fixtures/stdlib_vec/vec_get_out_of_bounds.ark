use std::test

fn main() {
    let mut v: Vec<i32> = Vec_new_i32()
    push(v, 10)
    push(v, 20)
    match get(v, 5) {
        Option::Some(val) => panic("expected None, got Some"),
        None => {
        },
    }
    match get(v, 100) {
        Option::Some(val) => panic("expected None, got Some"),
        None => {
        },
    }
}
