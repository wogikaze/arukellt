use std::test

fn is_five(x: i32) -> bool {
    x == 5
}

fn main() {
    let mut v: Vec<i32> = Vec_new_i32()
    push(v, 5)
    push(v, 10)
    push(v, 15)
    test::assert_true(any_i32(v, is_five))
}
