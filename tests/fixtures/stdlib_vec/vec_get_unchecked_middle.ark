use std::test

fn main() {
    let mut v: Vec<i32> = Vec_new_i32()
    push(v, 100)
    push(v, 200)
    push(v, 300)
    push(v, 400)
    push(v, 500)
    test::assert_eq_i32(get_unchecked(v, 2), 300)
}
