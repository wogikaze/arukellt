use std::test

fn is_positive(x: i32) -> bool {
    x > 0
}

fn main() {
    let mut v: Vec<i32> = Vec_new_i32()
    push(v, 1)
    push(v, 2)
    push(v, 3)
    test::assert_true(any_i32(v, is_positive))
}
