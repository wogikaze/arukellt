use std::test

fn main() {
    let mut v: Vec<i64> = Vec_new_i64()
    let a: i64 = 100
    let b: i64 = 200
    push(v, a)
    push(v, b)
    test::assert_eq_i32(len(v), 2)
    test::assert_eq_i64(get_unchecked(v, 0), 100)
    test::assert_eq_i64(get_unchecked(v, 1), 200)
}
