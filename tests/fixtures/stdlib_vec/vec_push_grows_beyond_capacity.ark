use std::test

fn main() {
    let mut v: Vec<i32> = Vec_new_i32_with_cap(2)
    push(v, 1)
    push(v, 2)
    push(v, 3)
    push(v, 4)
    push(v, 5)
    test::assert_eq_i32(len(v), 5)
    test::assert_eq_i32(get_unchecked(v, 0), 1)
    test::assert_eq_i32(get_unchecked(v, 4), 5)
}
