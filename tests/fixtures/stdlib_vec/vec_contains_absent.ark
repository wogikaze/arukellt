use std::test

fn main() {
    let mut v: Vec<i32> = Vec_new_i32()
    push(v, 10)
    push(v, 20)
    push(v, 30)
    test::assert_false(contains_i32(v, 99))
    test::assert_false(contains_i32(v, 0))
}
