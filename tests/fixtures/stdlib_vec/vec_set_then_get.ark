use std::test

fn main() {
    let mut v: Vec<i32> = Vec_new_i32()
    push(v, 10)
    push(v, 20)
    push(v, 30)
    set(v, 1, 777)
    match get(v, 1) {
        Option::Some(val) => test::assert_eq_i32(val, 777),
        None => panic("expected Some, got None"),
    }
}
