use std::bytes
use std::test

fn main() {
    let le: Vec<i32> = bytes::u16_to_le_bytes(513)
    test::assert_eq_i32(bytes::bytes_len(le), 2)
    test::assert_eq_i32(bytes::bytes_get(le, 0), 1)
    test::assert_eq_i32(bytes::bytes_get(le, 1), 2)
}
