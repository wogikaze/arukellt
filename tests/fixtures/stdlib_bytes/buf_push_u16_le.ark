use std::bytes
use std::test

fn main() {
    let buf: Vec<i32> = bytes::buf_new()
    bytes::buf_push_u16_le(buf, 258)
    test::assert_eq_i32(bytes::buf_len(buf), 2)
    test::assert_eq_i32(bytes::bytes_get(buf, 0), 2)
    test::assert_eq_i32(bytes::bytes_get(buf, 1), 1)
}
