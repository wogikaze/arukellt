use std::bytes
use std::test

fn main() {
    let b: Vec<i32> = bytes::bytes_new()
    bytes::bytes_push(b, 1)
    bytes::bytes_push(b, 2)
    bytes::bytes_push(b, 3)
    let s: Vec<i32> = bytes::bytes_slice(b, 1, 1)
    test::assert_eq_i32(bytes::bytes_len(s), 0)
}
