use std::bytes
use std::test

fn assert_none(v: Option<i32>) {
    match v {
        Option::Some(_) => panic("expected None"),
        None => {
        },
    }
}

fn main() {
    assert_none(bytes::try_hex_val_char("g"))
    assert_none(bytes::try_hex_val_char("z"))
}
