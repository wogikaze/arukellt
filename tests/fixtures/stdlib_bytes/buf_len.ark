use std::bytes
use std::test

fn main() {
    let buf: Vec<i32> = bytes::buf_new()
    test::assert_eq_i32(bytes::buf_len(buf), 0)
    bytes::buf_push_u8(buf, 1)
    test::assert_eq_i32(bytes::buf_len(buf), 1)
}
