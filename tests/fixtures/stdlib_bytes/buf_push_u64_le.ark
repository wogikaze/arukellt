use std::bytes
use std::test

fn main() {
    let buf: Vec<i32> = bytes::buf_new()
    bytes::buf_push_u64_le(buf, i32_to_i64(1))
    test::assert_eq_i32(bytes::buf_len(buf), 8)
    test::assert_eq_i32(bytes::bytes_get(buf, 0), 1)
    test::assert_eq_i32(bytes::bytes_get(buf, 7), 0)
}
