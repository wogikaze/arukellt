use std::host::stdio

fn main() {
    let mut outer: Vec<Vec<i32>> = Vec_new_i32()
    let mut inner: Vec<i32> = Vec_new_i32()
    push(inner, 3)
    push(inner, 4)
    push(outer, inner)
    let got = get_unchecked(outer, 0)
    stdio::println((get_unchecked(got, 1)).to_string())
}
