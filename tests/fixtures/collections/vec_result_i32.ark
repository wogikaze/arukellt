use std::host::stdio

fn main() {
    let xs: Vec<Result<i32, String>> = Vec::new<i32>()
    push(xs, Ok(9))
    match get_unchecked(xs, 0) {
        Ok(n) => stdio::println(n.to_string()),
        Err(_) => stdio::println(String_from("err")),
    }
}
