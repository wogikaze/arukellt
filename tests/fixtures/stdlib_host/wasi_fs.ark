// Fixture: wasi_fs.ark
// Verifies std::host::fs read-probe on a known fixture file.
use std::host::fs
use std::host::stdio

fn main() {
    let ok = fs::is_readable_file("tests/fixtures/stdlib_fs/present.txt")
    stdio::println((ok).to_string())
}
