use std::core::ops
use std::test

fn main() {
    test::assert_eq_i32((10).add(32), 42)
    test::assert_eq_i32((0).add(0), 0)
    test::assert_eq_i32((-5).add(5), 0)
    test::assert_eq_i32((-5).add(-3), -8)
}
