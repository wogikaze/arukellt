use std::core::iter
use std::test

fn main() {
    let v: Vec<i32> = Vec_new_i32()
    push(v, 10)
    push(v, 20)
    push(v, 30)

    test::assert_eq_i32(iter::iter_sum_i32(v), 60)

    let empty: Vec<i32> = Vec_new_i32()
    test::assert_eq_i32(iter::iter_sum_i32(empty), 0)
}
