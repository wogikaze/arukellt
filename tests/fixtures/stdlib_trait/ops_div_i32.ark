use std::core::ops
use std::test

fn main() {
    test::assert_eq_i32((42).div(7), 6)
    test::assert_eq_i32((0).div(1), 0)
    test::assert_eq_i32((-42).div(7), -6)
    test::assert_eq_i32((-42).div(-7), 6)
}
