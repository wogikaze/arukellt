use std::io
use std::test

fn main() {
    let buf = io::new_memory_buffer()
    let read = io::read_bytes(buf, 10)
    test::assert_eq_i32(len(read), 0)

    let pos: i64 = io::position(buf)
    test::assert_eq_i64(pos, 0)
}
