use std::core::iter
use std::test

fn main() {
    let v: Vec<i32> = Vec_new_i32()
    push(v, 1)
    push(v, 2)
    push(v, 3)

    let skipped = iter::collect_skip_i32(v, 10)
    test::assert_eq_i32(len(skipped), 0)
}
