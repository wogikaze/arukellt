use std::text::fmt
use std::test

fn main() {
    test::assert_eq_string(fmt::format_debug('A'), "A")
    test::assert_eq_string(fmt::format_debug('z'), "z")
    test::assert_eq_string(fmt::format_debug('0'), "0")
    test::assert_eq_string(fmt::write_debug_to("c=", 'X'), "c=X")
}
