use std::core::cmp
use std::test

fn main() {
    let o = (10).cmp(3)
    match o {
        Ordering::Greater => {}
        Ordering::Less => panic("expected Greater"),
        Ordering::Equal => panic("expected Greater"),
    }
    test::assert_true(true)
}
