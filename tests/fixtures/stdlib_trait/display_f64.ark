use std::core::convert
use std::test

fn main() {
    test::assert_eq_string(to_string(3.14), f64_to_string(3.14))
    test::assert_eq_string(to_string(0.0), f64_to_string(0.0))
    test::assert_eq_string(to_string(-2.5), f64_to_string(-2.5))
}
