use std::core::cmp
use std::test

fn main() {
    test::assert_true(eq(42, 42))
    test::assert_false(eq(42, 43))
    test::assert_true(eq(0, 0))
    test::assert_true(eq(-1, -1))
    test::assert_false(eq(-1, 1))
}
