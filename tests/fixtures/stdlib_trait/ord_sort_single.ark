use std::seq
use std::test

fn main() {
    let v: Vec<i32> = Vec_new_i32()
    push(v, 42)
    let s = seq::sort_by(v)
    test::assert_eq_i32(len(s), 1)
    test::assert_eq_i32(get_unchecked(s, 0), 42)
}
