use std::core::iter
use std::test

fn main() {
    let v: Vec<i32> = Vec_new_i32()
    push(v, 10)
    push(v, 20)
    push(v, 30)

    let it = iter::vec_iter(v)
    let mut total: i32 = 0
    for x in it {
        total = total + x
    }
    test::assert_eq_i32(total, 60)
}
