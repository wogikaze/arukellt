use std::core::ops
use std::test

fn main() {
    test::assert_eq_i32((12).bitxor(10), 6)
    test::assert_eq_i32((255).bitxor(255), 0)
    test::assert_eq_i32((0).bitxor(0), 0)
    test::assert_eq_i32((0).bitxor(1), 1)
}
