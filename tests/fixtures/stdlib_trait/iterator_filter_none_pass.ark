use std::core::iter
use std::test

fn always_false(x: i32) -> bool { false }

fn main() {
    let v: Vec<i32> = Vec_new_i32()
    push(v, 10)
    push(v, 20)
    push(v, 30)

    let filtered = iter::collect_filter_i32(v, always_false)
    test::assert_eq_i32(len(filtered), 0)
}
