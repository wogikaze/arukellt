use std::core::iter
use std::test

fn main() {
    let v: Vec<i32> = Vec_new_i32()
    push(v, 1)
    push(v, 2)
    push(v, 3)
    push(v, 4)

    test::assert_eq_i32(iter::iter_count_i32(v), 4)

    let empty: Vec<i32> = Vec_new_i32()
    test::assert_eq_i32(iter::iter_count_i32(empty), 0)
}
