use std::text::fmt
use std::test

fn main() {
    test::assert_eq_string(fmt::format_debug(String_from("hello")), "\"hello\"")
    test::assert_eq_string(fmt::format_debug(String_from("world")), "\"world\"")
    test::assert_eq_string(fmt::write_debug_to("s=", String_from("hi")), "s=\"hi\"")
}
