use std::core::iter
use std::test

fn is_even(x: i32) -> bool { x / 2 * 2 == x }

fn main() {
    let v: Vec<i32> = Vec_new_i32()
    push(v, 1)
    push(v, 3)
    push(v, 5)
    push(v, 8)

    test::assert_true(iter::iter_any_i32(v, is_even))
}
