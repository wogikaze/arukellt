use std::host::env
use std::host::stdio

fn main() {
    stdio::println(i32_to_string(env::arg_count()))
    stdio::println(bool_to_string(env::has_flag("--verbose")))
    match env::arg_at(0) {
        Option::Some(s) => stdio::println(s),
        None => stdio::println("no arg"),
    }
}
