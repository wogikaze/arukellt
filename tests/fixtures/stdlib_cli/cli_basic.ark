use std::host::env
use std::host::stdio

fn main() {
    stdio::println((env::arg_count()).to_string())
    stdio::println((env::has_flag("--verbose")).to_string())
    match env::arg_at(0) {
        Option::Some(s) => stdio::println(s),
        None => stdio::println("no arg"),
    }
}
