fn make_label() -> String {
    String_from("label")
}

fn main() -> i32 {
    let value = make_label()
    let mut i = 0
    while i < 32 {
        let _junk = String_from("pad")
        i = i + 1
    }
    if eq(value, String_from("label")) { 0 } else { 1 }
}
