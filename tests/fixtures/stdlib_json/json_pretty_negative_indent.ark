use std::json
use std::test

fn main() {
    match json::parse("[1,2]") {
        Result::Ok(v) => test::assert_eq_string(json::stringify_pretty(v, -1), "[\n1,\n2\n]"),
        Result::Err(e) => panic(concat("parse failed: ", json::parse_error_message(e))),
    }
}
