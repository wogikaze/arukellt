use std::json
use std::test

fn main() {
    match json::parse("") {
        Result::Ok(_) => panic("expected error for empty input"),
        Result::Err(e) => match e {
            json::JsonParseError::EmptyInput => {},
            _ => panic("expected EmptyInput error"),
        },
    }
}
