use std::host::stdio
use std::json

fn main() {
    match json::parse("[1,2,3]x") {
        Result::Ok(v) => stdio::println(json::stringify(v)),
        Result::Err(e) => stdio::println(concat("error:", e)),
    }
}
