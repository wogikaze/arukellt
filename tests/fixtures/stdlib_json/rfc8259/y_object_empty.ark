use std::json
use std::test

fn main() {
    match json::parse("{}") {
        Result::Ok(v) => test::assert_eq_bool(json::is_object(v), true),
        Result::Err(e) => panic(concat("parse empty object failed: ", json::parse_error_message(e))),
    }
}
