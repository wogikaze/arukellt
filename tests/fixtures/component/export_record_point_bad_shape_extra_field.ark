// The Point adapter only supports record point { x: s32, y: s32 }.
// Extra fields must be rejected before the fixed-layout adapter is selected.
struct Point {
    x: i32,
    y: i32,
    z: i32,
}

pub fn distance_sq(p: Point) -> i32 {
    p.x * p.x + p.y * p.y + p.z * p.z
}

fn main() {
}
