// Export with Result<i32, i32> parameter and result maps to WIT result<s32, s32> roundtrip shape.
pub fn bump_ok(r: Result<i32, i32>) -> Result<i32, i32> {
    let n = unwrap_or(r, 0)
    if n > 0 {
        Ok(n + 1)
    } else {
        Err(7)
    }
}
