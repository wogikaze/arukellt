// Export with struct (record) — maps to WIT record
// Fixture-specific adapter flattens Point fields to scalars.
struct Point {
    x: i32,
    y: i32,
}

pub fn distance_sq(p: Point) -> i32 {
    p.x * p.x + p.y * p.y
}

fn main() {
}
