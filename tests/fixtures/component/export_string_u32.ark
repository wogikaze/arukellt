// Export with String parameter and u32 result maps to WIT string -> u32.

pub fn count32_text(text: String) -> u32 {
    if len(text) == 0 {
        return 32u32
    }
    123456u32
}

fn main() {
}
