// Export with renamed String parameter and u64 result maps to WIT string -> u64.

pub fn large_unsigned(text: String) -> u64 {
    if len(text) == 0 {
        return 42u64
    }
    9000000000u64
}

fn main() {
}
