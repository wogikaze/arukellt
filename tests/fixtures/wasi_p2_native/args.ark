use std::host::stdio
use std::host::env

fn main() {
    let values = env::args()
    let mut i = 0
    while i < len(values) {
        stdio::println(get_unchecked(values, i))
        i = i + 1
    }
}
