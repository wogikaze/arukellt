use std::host::stdio
use std::test
fn main() {
    for i in 0..20 {
        let r: Result<i32, i32> = Ok(i)
        test::assert_true(is_ok(r))
        test::assert_false(is_err(r))
    }
    stdio::println("ok")
}
