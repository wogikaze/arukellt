use std::host::stdio
use std::test
fn main() {
    let a: Option<Option<i32>> = None
    match a {
        Option::Some(inner) => panic("outer some"),
        None => {},
    }
    stdio::println("ok")
}
