use std::host::stdio
use std::test
fn double(x: i32) -> i32 { x * 2 }
fn main() {
    let a: Option<i32> = Some(0)
    let m = map_option_i32_i32(a, double)
    match m { Option::Some(v) => test::assert_eq_i32(v, 0), None => panic("none") }
    stdio::println("ok")
}
