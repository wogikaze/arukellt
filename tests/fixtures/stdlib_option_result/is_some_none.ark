use std::host::stdio

fn main() {
    let a: Option<i32> = Some(1)
    let b: Option<i32> = None
    stdio::println((is_some(a)).to_string())
    stdio::println((is_none(a)).to_string())
    stdio::println((is_some(b)).to_string())
    stdio::println((is_none(b)).to_string())
}
