use std::host::stdio
use std::test
fn main() {
    let a: Option<i32> = Some(-7)
    test::assert_true(is_some(a))
    match a { Option::Some(v) => test::assert_eq_i32(v, -7), None => panic("none") }
    stdio::println("ok")
}
