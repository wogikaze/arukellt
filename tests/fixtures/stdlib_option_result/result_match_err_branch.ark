use std::host::stdio
use std::test
fn main() {
    let r: Result<i32, i32> = Err(7)
    match r {
        Result::Ok(v) => panic("ok"),
        Result::Err(e) => test::assert_eq_i32(e, 7),
    }
    stdio::println("ok")
}
