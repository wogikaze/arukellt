use std::host::stdio
use std::test
fn process(o: Option<i32>) -> i32 {
    match o { Option::Some(v) => v * 10, None => -1 }
}
fn main() {
    test::assert_eq_i32(process(Some(5)), 50)
    test::assert_eq_i32(process(None), -1)
    stdio::println("ok")
}
