use std::host::stdio
use std::test
fn main() {
    for i in 0..20 {
        let r: Result<i32, i32> = Err(i)
        test::assert_true(is_err(r))
        test::assert_false(is_ok(r))
    }
    stdio::println("ok")
}
