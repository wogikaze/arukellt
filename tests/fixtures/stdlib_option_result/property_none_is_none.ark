use std::host::stdio
use std::test
fn main() {
    for i in 0..20 {
        let opt: Option<i32> = None
        test::assert_true(is_none(opt))
        test::assert_false(is_some(opt))
    }
    stdio::println("ok")
}
