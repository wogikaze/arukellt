use std::host::stdio
use std::test
fn make_err(e: i32) -> Result<i32, i32> {
    Err(e)
}
fn main() {
    let r = make_err(7)
    test::assert_true(is_err(r))
    match r { Result::Ok(v) => panic("ok"), Result::Err(e) => test::assert_eq_i32(e, 7) }
    stdio::println("ok")
}
