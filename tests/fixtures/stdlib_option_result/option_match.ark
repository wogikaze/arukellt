use std::host::stdio

fn describe(opt: Option<i32>) -> String {
    match opt {
        Option::Some(val) => concat(String_from("got "), (val).to_string()),
        None => String_from("nothing"),
    }
}

fn main() {
    stdio::println(describe(Some(42)))
    stdio::println(describe(None))
}
