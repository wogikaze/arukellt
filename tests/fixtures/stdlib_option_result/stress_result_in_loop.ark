use std::host::stdio
use std::test
fn main() {
    let mut sum: i32 = 0
    for i in 1..11 {
        let r: Result<i32, i32> = Ok(i)
        match r { Result::Ok(v) => sum = sum + v, Result::Err(e) => {} }
    }
    test::assert_eq_i32(sum, 55)
    stdio::println("ok")
}
