use std::host::stdio
use std::test
fn main() {
    for i in 0..20 {
        let r: Result<i32, i32> = Err(i)
        match r {
            Result::Ok(v) => panic("should be err"),
            Result::Err(e) => test::assert_eq_i32(e, i),
        }
    }
    stdio::println("ok")
}
