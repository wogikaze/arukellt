use std::host::stdio
use std::test
fn main() {
    let r: Result<i32, i32> = Ok(42)
    match r {
        Result::Ok(v) => test::assert_eq_i32(v, 42),
        Result::Err(e) => panic("err"),
    }
    stdio::println("ok")
}
